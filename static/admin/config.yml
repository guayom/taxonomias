backend:
  name: github
  repo: guayom/taxonomias
  branch: master

media_folder: "static/images"
public_folder: "/images"

collections:
  - name: "especies" # Used in routes, e.g., /admin/collections/blog
    label: "Especies" # Used in the UI
    folder: "content/especies" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    editor:
      preview: false
    fields: # The fields for each document, usually in front matter
      - {label: "Reino", name: "reinos", widget: "list", required: false}
      - {label: "División", name: "divisiones", widget: "list", required: false}
      - {label: "Clase", name: "clases", widget: "list", required: false}
      - {label: "Órden", name: "ordenes", widget: "list", required: false}
      - {label: "Familia", name: "familias", widget: "list", required: false}
      - {label: "Género", name: "generos", widget: "list", required: false}
      - {label: "Nombre científico", name: "nombre_cientifico", widget: "string", required: false}
      - {label: "Nombre común", name: "nombre_comun", widget: "string", required: false}
      - {label: "Nombre inglés", name: "nombre_ingles", widget: "string", required: false}
      - {label: "Especie", name: "title", widget: "string"}
      - {label: "Otros nombres", name: "otros_nombres", widget: "list", required: false}
      - {label: "Links", name: "links", widget: "string"}
      # - {label: "Notas", name: "notas", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown", required: false}
      - {label: "Draft", name: "draft", widget: "hidden", default: "false", required: false}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - name: images
        label: Imágenes
        widget: list
        fields:
          - {label: URL de la imagen, name: url, widget: string}
